# ── WeathCionz environment ────────────────────────────────────────────────
# Copy this file to `.env`. All variables are optional.

# Weather & forecast data come from Open-Meteo (free, no key required) and
# always match the exact coordinates of the selected location. Live data
# works out of the box with no key.

# OpenWeatherMap is used for map tiles and geocoding.
# Get a free key at https://openweathermap.org/api
VITE_OWM_KEY=

# Optional — a backend proxy (e.g. the included Vercel function in /api).
# Set to your deployed URL to keep the key server-side in production, e.g.
#   VITE_API_BASE=https://your-project.vercel.app/api
# Leave blank to call OpenWeatherMap directly (development only).
VITE_API_BASE=
